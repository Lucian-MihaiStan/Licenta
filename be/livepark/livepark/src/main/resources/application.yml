spring:
  datasource:
    username: lic_livepark
    password: lic_livepark
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/lic_livepark

  seconddatasource:
    username: lic_livepark_cars
    password: lic_livepark_cars
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5433/lic_livepark_cars

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: true
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect

live:
  park:
    app:
      jwtSecret: liveParkSecretKeyliveParkSecretKeyliveParkSecretKeyliveParkSecretKeyliveParkSecretKeylive
      jwtExpirationMs: '86400000'