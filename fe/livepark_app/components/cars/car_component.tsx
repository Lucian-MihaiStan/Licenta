import React from "react";
import { forwardRef } from "react";
import { CarCardProps } from "./car_card_props";

export const CarCard = forwardRef((props: CarCardProps) => {

    return (
        <div>
            
        </div>
    )

});